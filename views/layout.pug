html
  head
    title= `${title || 'Node Webserver'}`
    meta(name="viewport" content="width=device-width, initial-scale=1")
    meta(charset="utf-8")
  body
    block content
      h1 #{title}
      p Lorem ipsum dolor sit amet consectetur adipisicing elit. Quas, ad.

    footer
      p Copyright #{currentYear}